apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sgbi.fullname" . }}-test-connection"
  labels:
    {{- include "sgbi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sgbi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
