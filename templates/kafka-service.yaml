apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kafka
  namespace: {{ .Values.kafka.namespaceOverride }}
spec:
  ports:
    - port: 9092
      targetPort: 9092
  selector:
    app: kafka
